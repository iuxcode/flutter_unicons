import 'package:flutter_test/flutter_test.dart';

import 'package:flutter_unicons/flutter_unicons.dart';

void main() {
  testWidgets('test ...', (WidgetTester tester) async {
    await tester.pumpWidget(Unicon(Unicons.uni0Plus));
  });
}
